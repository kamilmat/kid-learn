node_modules
dist
dist-ssr
.superpowers/
.DS_Store
coverage
*.local
.vite
.env
.env.*
!.env.example

# Piper TTS local models (large, downloaded on demand)
.piper-models/

# tsbuildinfo (regenerable)
*.tsbuildinfo

# Audio recorder source files (kept locally only; MP3 wersje commitowane)
audio-source/manual-overrides/*.webm
